# プロジェクトの指針

## 開発

エージェントは `yuru` コマンドおよび `yuru latest` を含むすべてのサブコマンドを実行してはならない。インストール済みのコマンドは、ユーザーが普段使いする Yuru 用。そのアプリをインストール・置換する `install.sh` と `npm run package:local` も実行禁止。開発中の動作確認には、以下のタスク用ワークツリーでのビルド・再起動コマンドを使う。

起動中の Yuru アプリで確認できる挙動に影響する変更をした場合は、リポジトリのルートではなく、作業中のタスク用ワークツリーでビルドし直して再起動する。

```sh
npm run build
npm run app:restart
```

## E2E テスト

Electron/Playwright の E2E テストは、実際の macOS GUI アプリを起動する。Codex では通常のサンドボックス内で実行せず、承認を得てサンドボックス外で `npm run test:e2e` を実行する。`CODEX_SANDBOX=seatbelt` の環境で `_electron.launch()` を実行すると、macOS へのアプリ登録中に Electron が異常終了し、システムのクラッシュ報告・再起動ダイアログが残ることがある。

E2E テストでは、既定で BrowserWindow を非表示にする（`YURU_E2E_HIDE_WINDOW=1`）。画面を見ながらのデバッグが必要な場合に限り、`YURU_E2E_SHOW_WINDOW=1 npm run test:e2e -- ...` を使う。

失敗した E2E テストは 1 回再試行する（`retries: 1`）。再試行で成功したテストは、テスト実行全体の失敗ではなく、不安定なテスト（flaky）として報告される。機能の開発中は不安定なテストを調査せず、テスト名を報告して作業を続ける。不安定さの調査は別の作業として、`--repeat-each` でテストスイートを繰り返し実行し、各失敗の原因を追う。

実際の Claude を使う E2E テストは、macOS キーチェーンからユーザーの現在の Claude Code ログイン情報を借りる。`Login expired` または `401 OAuth access token has been revoked` が出た場合は、通常の Claude Code セッションで `/login` を実行するようユーザーに依頼し、完了の返答を得てから再実行する。ログインを自動化したり、ユーザーに代わってキーチェーンの認証情報を変更したりしてはならない。テスト用の認証情報を準備する際に出る macOS キーチェーンのアクセス確認ダイアログは、ログイン期限切れとは別の権限確認である。

## ドキュメント

継続的にメンテされる最新情報は次の 4 つだけ:

- 目的: `docs/purpose.md`
- プロダクトのバックログ: `docs/backlog.md`
- アーキテクチャの記録: `docs/architecture.md`
- コーディング指針: `docs/coding-guidelines.md`

それ以外のドキュメント（ADR を含む）は書いた時点での調査・設計・検討の記録。現在の実装とズレていても更新しない。現在の設計として残すべき内容は `docs/architecture.md` などメンテ対象のドキュメントに書く。

`docs/backlog.md` には、ユーザーの許可なく項目を追加しない。作業中に見つけた「別途やるべきこと」は、その場で報告するだけにとどめる。優先度と粒度はユーザーが決める。

## コミュニケーション

- 読み手はコードをざっくりとしか読んでいない前提で、設計やバグの説明は「つまりどういうことか」から伝える。変数名・関数名を出すときは、それが何を表すかを添える。
- 独自用語を作らず、既存のコード・ドキュメント・ユーザーの言葉に合わせる。

## 設計

- 現在の要件を満たす、最もシンプルな設計を選ぶ（YAGNI）。
- 原因を持つ責務の範囲で問題を直す。既存設計が妨げになる場合は、その設計も見直す。
- 失敗時の挙動はプロダクトの仕様として扱う。外部依存の契約と実際に起こる失敗条件を根拠に設計する。
- 状態の正となる情報源は一つに保ち、導出できる値は計算する。
- 既存設計を大きく変える場合や、失敗時のユーザー向け挙動が未決定の場合は、根拠・影響・選択肢を示して実装前に相談する。通常の実装判断は既存の仕様と設計に沿って進める。
